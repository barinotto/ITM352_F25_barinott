# Create the list of responses
responses = [5, 7, 3, 8]

# Add "0" to the end of the list
responses.append(0)

# Insert "6" at the third position (index 2, since Python is zero-based)
responses.insert(2, 6)

# Print the list to verify the changes
print(responses)